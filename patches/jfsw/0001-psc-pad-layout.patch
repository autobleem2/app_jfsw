diff --git a/src/_functio.h b/src/_functio.h
index 967a77a..702c5d3 100644
--- a/src/_functio.h
+++ b/src/_functio.h
@@ -240,23 +240,27 @@ static char * mouseclickeddefaults_modern[MAXMOUSEBUTTONS] =
    };
 
 
+// AutoBleem: the PlayStation Classic port's layout (2020) - Square fire, Cross crouch (double press: run mode),
+// Circle open, Triangle jump, Select use item, Start menu (double press: map), L1 next item, R1 next weapon,
+// L2/R2 strafe (the digital axes below), the D-pad and the left stick move and turn. The classic set put the
+// D-pad on aiming and looking, which leaves the console's own pad - a D-pad and no sticks - unable to move.
 static char * joystickdefaults[MAXJOYBUTTONS] =
    {
-   "Fire",        // A
-   "Strafe",      // B
-   "Run",         // X
-   "Open",        // Y
-   "",            // Back
+   "Crouch",      // A - Cross
+   "Open",        // B - Circle
+   "Fire",        // X - Square
+   "Jump",        // Y - Triangle
+   "Inventory",   // Back - Select
    "",            // Guide
-   "",            // Start
+   "Show_Menu",   // Start
    "",            // L thumb
    "",            // R thumb
-   "",            // L shoulder
-   "",            // R shoulder
-   "Aim_Up",      // DP up
-   "Aim_Down",    // DP down
-   "Look_Left",   // DP left
-   "Look_Right",  // DP right
+   "Inventory_Right", // L shoulder - L1
+   "Next_Weapon", // R shoulder - R1
+   "Move_Forward",  // DP up
+   "Move_Backward", // DP down
+   "Turn_Left",   // DP left
+   "Turn_Right",  // DP right
    "",
    "",
    "",
@@ -279,13 +283,13 @@ static char * joystickdefaults[MAXJOYBUTTONS] =
 
 static char * joystickclickeddefaults[MAXJOYBUTTONS] =
    {
-   "",            // A
-   "Inventory",   // B
-   "Jump",        // X
-   "Crouch",      // Y
+   "AutoRun",     // A - Cross, double press
+   "",            // B
+   "",            // X
+   "",            // Y
    "",            // Back
    "",            // Guide
-   "",            // Start
+   "Map",         // Start, double press
    "",            // L thumb
    "",            // R thumb
    "",            // L shoulder
@@ -409,7 +413,7 @@ static char * joystickanalogdefaults[MAXJOYAXES] =
    "analog_turning",
    "analog_moving",
    "analog_strafing",
-   "",
+   "analog_lookingupanddown",
    "",
    "",
    "",
@@ -421,6 +425,7 @@ static char * joystickanalogdefaults[MAXJOYAXES] =
    };
 
 
+// two per axis, negative then positive: the triggers (axes 4 and 5, pressed = positive) strafe
 static char * joystickdigitaldefaults[MAXJOYAXES*2] =
    {
    "",
@@ -429,12 +434,12 @@ static char * joystickdigitaldefaults[MAXJOYAXES*2] =
    "",
    "",
    "",
-   "Run",
-   "",
    "",
    "",
    "",
+   "Strafe_Left",  // L2
    "",
+   "Strafe_Right", // R2
    "",
    "",
    "",
