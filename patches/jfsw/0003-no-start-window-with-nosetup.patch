diff --git a/src/startwin_game.c b/src/startwin_game.c
index 1fd60bf..cd4eae2 100644
--- a/src/startwin_game.c
+++ b/src/startwin_game.c
@@ -803,6 +803,11 @@ int startwin_open(void)
 
     if (startupdlg) return 1;
 
+    // AutoBleem: with -nosetup the window is not wanted at all - not even as the start-up log that flashes
+    // on screen for a moment before the game (it is opened before the arguments are read, hence the check
+    // on the raw command line); everything else here copes with there being no window
+    if (strstr(GetCommandLineA(), "-nosetup")) return 1;
+
     icc.dwSize = sizeof(icc);
     icc.dwICC = ICC_TAB_CLASSES | ICC_UPDOWN_CLASS | ICC_LISTVIEW_CLASSES | ICC_LINK_CLASS;
     InitCommonControlsEx(&icc);
